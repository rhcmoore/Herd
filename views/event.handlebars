<h3>Event info</h3>
<div class="container">
    <div class="row">
        <div>
            {{#each Event}}
            <div>Event name: {{name}}</div>
            <div>Event description: {{description}}</div>
            <div>Event time: {{date}}</div>
            {{/each}}
        </div>
    </div>
</div>